# tuple - кортеж
numbers_tuple = (1, 2, 3, 4, 5)


"""
length_of_tuple = len(my_tuple) длина кортежа
min(my_tuple) минимум кортежа
max(my_tuple) максимум кортежа
tuple(sorted(original_tuple)) - сортировка кортежа
tuple(sorted(original_tuple, reverse=True)) - сортировка кортежа
my_string = ''.join(map(str, my_tuple)) - преобразование в строку
"""